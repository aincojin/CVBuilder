import { SelectItemsDisplayPipe } from './select-items-display.pipe';

describe('SelectItemsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectItemsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
