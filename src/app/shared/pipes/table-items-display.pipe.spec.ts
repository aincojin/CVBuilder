import { TableItemsDisplayPipe } from './table-items-display.pipe';

describe('TableItemsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TableItemsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
